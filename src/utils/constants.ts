// canvas 元素类型
export enum CANVAS_ELE_TYPE {
  FREE_LINE = 'freeLine',
  CLEAN_LINE = 'cleanLine'
}

// 通用绘画宽度
export const CommonWidth = {
  W5: 4,
  W10: 8,
  W15: 12,
  W20: 14
}

// 按键枚举
export enum KeyCode {
  SPACE = 'Space'
}
