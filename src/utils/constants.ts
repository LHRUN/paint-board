// canvas 元素类型
export enum CANVAS_ELE_TYPE {
  FREE_LINE = 'freeLine',
  CLEAN_LINE = 'cleanLine'
}

// 通用绘画宽度
export const CommonWidth = {
  W5: 6,
  W10: 12,
  W15: 16,
  W20: 22
}

// 按键枚举
export enum KeyCode {
  SPACE = 'Space'
}
